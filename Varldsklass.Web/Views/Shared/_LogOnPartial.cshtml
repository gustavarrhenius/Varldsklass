@if(Request.IsAuthenticated) {
    <text>Välkommen, <strong>@User.Identity.Name</strong>!
     @Html.ActionLink("Logga ut", "LogOff", "Account", null,  new { @class = "success button" }) </text>
}
else
{
    @: @Html.ActionLink("Logga in", "LogOn", "Account", null, new { @class = "success button" }) 
}
