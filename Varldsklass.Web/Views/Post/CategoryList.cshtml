@model IEnumerable<Varldsklass.Domain.Entities.Category>

@{
    ViewBag.Title = "CategoryList";
}

<h2>Utbildningar</h2>

<ul>
@foreach (var category in Model) {
    <li>@Html.ActionLink(category.Name, "CourseList", "Course", new {id = category.ID }, null)</li>
}
 </ul>